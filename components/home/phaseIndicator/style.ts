import { StyleSheet } from "react-native";

export const phaseIndicatorStyles = StyleSheet.create({
  container: {
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#17a2b8",
    padding: 20,
    borderRadius: 5,
  },
  phaseText: {
    color: "#fff",
    fontSize: 16,
  },
  phaseNumber: {
    color: "#fff",
    fontSize: 24,
    fontWeight: "bold",
  },
});
